# Copiloto Administrativo Agéntico — Universidad de Antioquia
# Python 3.11+

# Orquestación de agentes
langgraph>=0.2
langchain>=0.3
langchain-text-splitters>=0.3  # RecursiveCharacterTextSplitter
langchain-groq>=0.2          # LLM gratuito vía Groq (llama-3.3-70b)
langchain-openai>=0.3        # Solo necesario si usas OPENAI_API_KEY para embeddings

# Base de datos vectorial
chromadb>=0.5

# Interfaz web
gradio>=4.40

# Cliente HTTP
httpx>=0.27

# Procesamiento de PDFs
pymupdf>=1.24
pdfplumber>=0.11

# Embeddings fallback gratuito (se usa automáticamente sin OPENAI_API_KEY)
sentence-transformers>=3.0

# Testing
hypothesis>=6.100
pytest>=8.0

# Utilidades
python-dotenv>=1.0
